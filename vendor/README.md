# Vendored Code

This folder contains git checkouts of any relevant repositories that you might want to edit, including the `mngr` repository, which contains the `mngr` command, many of its plugins, and the `minds` app.
