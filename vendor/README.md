# Vendored Code

This folder contains git checkouts of any relevant repositories that you might want to edit, including the `mng` repository, which contains the `mng` command, many of its plugins, and the `minds` app.
